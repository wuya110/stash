tiles:
  # -----------------------------------------------------
  # 📺 小电视磁贴配置
  # -----------------------------------------------------
  - name: tv-switcher-01 
    label: 📺 我的小电视
    # 深蓝色背景
    backgroundColor: "#203040" 
    # 通用图标
    icon: globe
    iconColor: "#FFFFFF"
    # 刷新频率设为0，只在点击时运行 (手动换台)
    interval: 0 
    # 必须显式连接到下面的 'tv-script' 提供者
    script-provider-name: tv-script 
    argument: |-
        {"channelIndex": 0}

script-providers:
  # -----------------------------------------------------
  # 📺 小电视脚本提供者
  # -----------------------------------------------------
  tv-script:
    # 您的 GitHub RAW 链接
    url: https://raw.githubusercontent.com/wuya110/stash/refs/heads/main/tv_tile.js 
    interval: 86400 # 脚本文件每天检查更新
