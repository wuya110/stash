# --------------------------
# 磁贴定义 (tiles)
# --------------------------
tiles:
  # 更改 name 以确保唯一性，并使用一个通用图标
  - name: tv-switcher-01 
    label: 📺 我的小电视
    # 模拟电视机边框的颜色，深蓝色
    backgroundColor: "#203040" 
    # 更改 SF Symbol 图标，使用更通用的网络图标
    icon: globe
    # 图标颜色为白色
    iconColor: "#FFFFFF"
    # 刷新频率设为0，只在点击时运行
    interval: 0 
    # 关键：仍然链接到 'tv-script' 提供者
    script-provider-name: tv-script 
    # 传递给脚本的参数，用于保存当前频道状态
    argument: |-
        {"channelIndex": 0}

# --------------------------
# 脚本提供者定义 (script-providers)
# --------------------------
script-providers:
  # 小电视脚本提供者 (名称保持不变)
  tv-script:
    # 您的 GitHub RAW 链接
    url: https://raw.githubusercontent.com/wuya110/stash/refs/heads/main/tv_tile.js 
    interval: 86400
